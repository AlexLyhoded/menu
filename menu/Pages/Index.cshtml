@page
@model IndexModel
<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Головна - Кафе</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background: url('background.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .container {
            background: rgba(255, 255, 255, 0.8);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .btn-lg {
            font-size: 1.5rem;
            padding: 15px 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Ласкаво просимо до нашого кафе!</h1>
        <p class="lead">Оберіть, що ви хочете зробити:</p>
        <a class="btn btn-primary btn-lg me-3" href="/Menu">Меню</a>
    </div>
</body>
</html>
